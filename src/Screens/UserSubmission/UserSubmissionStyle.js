import { StyleSheet } from 'react-native'

export const style = StyleSheet.create({
    textInputStyle: {
        height: 40, 
        borderColor: 'red', 
        borderWidth: 1, 
        borderRadius: 10, 
        marginBottom: 20, 
        marginHorizontal: 8, 
        paddingHorizontal: 8
    }
  })